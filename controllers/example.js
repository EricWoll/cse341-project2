const db = require('../modles');
const model = db.model; // change 'model' out with actual model name

const apiKey = process.env.apiKey; // OAuth section (will be changed)

/*
    CRUD updates here
*/
